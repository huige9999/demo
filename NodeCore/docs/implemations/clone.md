# 目录复制

## 期望效果

把某个目录复制到另一个目录下，保持原有目录结构不变。


## 实现思路
1. 使用 `fs` 模块读取源目录下的所有文件和子目录。
2. 递归地遍历子目录，继续读取其中的文件和子目录。
3. 对于每个文件，使用 `fs.copyFileSync` 方法将其复制到目标目录下，保持原有目录结构不变。
4. 对于每个子目录，使用 `fs.mkdirSync` 方法在目标目录下创建相应的子目录，然后继续递归复制其中的内容。
5. 最终完成整个目录的复制。